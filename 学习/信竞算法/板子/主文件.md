---
tags:
  - 板子
---
# 主文件 1
```cpp
#include<bits/stdc++.h>
#define fast ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)
#define mid ((pl+pr)>>1)
#define rep(u,x,y) for(int u=x;u<=y;++u)
#define per(u,x,y) for(int u=x;u>=y;--u)
#define debug cout<<__LINE__<<" "<<__FUNCTION__<<endl;
#define lson(x) (x<<1)
#define rson(x) (x<<1|1)
#define ull unsigned long long
#define gc getchar
#define pc putchar
#define fp(name) freopen(#name ".in","r",stdin),freopen(#name ".out","w",stdout)
using namespace std;
const int N=1e6+1;
typedef long long ll;
ll read(){
    ll x=0,f=1;
    char ch=gc();
    while(ch<'0'||ch>'9')ch=='-'&&(f=-1,0),ch=gc();
    while(ch>='0'&&ch<='9')x=(x*10)+(ch^48),ch=gc();
    return x*f;
}
void wrt(ll x){
    if(x<0)x=-x,pc('-');
    if(x>9)wrt(x/10);
    pc(x%10^48);
}
int main(){

    return 0;
}
```